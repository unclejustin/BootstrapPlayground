describe('Filter: orcaCondition', function() {

	beforeEach(module('orca'));

	it('should ...', inject(function($filter) {

		//var filter = $filter('orcaCondition');
		//expect(filter('input')).toEqual('output');

	}));

});